# Local development overrides (not committed to git)
# Copy this file to .env.local and adjust values for your worktree
#
# Usage:
#   cp .env.local.example .env.local
#   # Edit .env.local with your desired port numbers
#
# This is useful when running multiple worktrees simultaneously,
# as each worktree can use different ports to avoid conflicts.

# =============================================================================
# PORT CONFIGURATION
# =============================================================================
# Default ports (main worktree):
#   Backend:  2000
#   Frontend: 5173
#   CMS:      5174
#
# Example for a second worktree (increment by 100):
#   Backend:  2100
#   Frontend: 5273
#   CMS:      5274

DEV_BACKEND_PORT=2000
DEV_FRONTEND_PORT=5173
DEV_CMS_PORT=5174

# =============================================================================
# API URL CONFIGURATION
# =============================================================================
# IMPORTANT: These must match DEV_BACKEND_PORT for frontend apps to connect
# to the correct backend instance.

VITE_BACKEND_URL=http://localhost:2000
VITE_API_URL=http://localhost:2000
