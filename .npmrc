# pnpm configuration
# Use workspace protocol for linking workspace packages
link-workspace-packages=true

# Hoist packages for better compatibility with tools
shamefully-hoist=true

# Auto-install peers (recommended for monorepos)
auto-install-peers=true
