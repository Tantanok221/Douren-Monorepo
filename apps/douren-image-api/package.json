{
  "name": "douren-image-api",
  "scripts": {
    "dev": "npx wrangler dev src/index.ts",
    "dev:backend": "npx wrangler dev src/index.ts",
    "deploy": "npx wrangler deploy --minify src/index.ts"
  },
  "dependencies": {
    "@repo/database": "*",
    "hono": "^4.5.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240806.0",
    "wrangler": "^3.72.2"
  }
}
